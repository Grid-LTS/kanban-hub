plugins {
    id 'groovy'
}

version '0.1.0'

repositories {
    mavenCentral()
}

dependencies {
    compile 'org.codehaus.groovy:groovy-all:3.0.1'
    implementation (project(':kh-base')) {
        transitive = true
    }
}
